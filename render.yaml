services:
  - type: web
    name: anon-bot
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: true
